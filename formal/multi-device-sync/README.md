# Multi-Device PreKey Sync

## What This Proves

Cross-device session sync via `IPreKeyStore`. Proves all devices
converge to the same pre-key set without conflicts.

| Property | Status |
|---|---|
| Convergence: all devices end up with same set | ✅ |
| No duplicate pre-key issuance | ✅ |

## Files

- `multi-device-sync.pnml` | `.q` | `properties.md` | `state-space.md`
