# Vault CPN — State Space

Per-doc state space: 8 markings (2^3 alive/dead per shard).
Per-doc properties hold structurally (place-flow invariant) without
enumerating combinatorial product across multiple docs.

CPN Tools' place-flow analysis proves recovery property for arbitrary
number of documents — scales beyond P/T enumeration.
