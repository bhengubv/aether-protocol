{
  "name": "aether-arkts-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node parity harness for the ArkTS port (NOT part of the HarmonyOS build). Bundles the .ets sources via esbuild and asserts byte-identity against the shared cross-language fixtures.",
  "scripts": {
    "parity": "tsx parity.test.ts",
    "typecheck": "tsx typecheck.ts"
  },
  "dependencies": {
    "@noble/ed25519": "^2.3.0",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.6.3"
  }
}
