# Force in-process Kotlin compilation — avoids "Could not connect to Kotlin
# compile daemon" failures that Gradle's daemon-less mode can trigger.
# The compile daemon is useful for incremental builds in long-lived IDE sessions;
# for CI and one-shot local builds the overhead is unnecessary.
kotlin.compiler.execution.strategy=in-process