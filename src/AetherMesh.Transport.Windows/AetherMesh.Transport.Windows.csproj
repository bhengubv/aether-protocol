<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
    <RootNamespace>AetherMesh.Transport.Windows</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <Description>Windows BLE GATT transport for the Aether mesh protocol. Uses WinRT Windows.Devices.Bluetooth APIs to act as a BLE central connecting to Aether-compatible peripherals (phones, Pi nodes).</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../AetherMesh.Transport/AetherMesh.Transport.csproj" />
    <ProjectReference Include="../AetherMesh.Core/AetherMesh.Core.csproj" />
    <ProjectReference Include="../AetherMesh.DependencyInjection/AetherMesh.DependencyInjection.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.2" />
  </ItemGroup>

</Project>
